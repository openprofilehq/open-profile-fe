name: Forbidden pattern scan

on:
  pull_request:
  push:
    branches: [main, staging, dev, production]

jobs:
  scan-forbidden-global-subscript:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Search repo for forbidden substring
        run: bash scripts/forbidden-pattern-scan.sh "${{ github.workspace }}" ":(exclude).github/workflows/forbidden-pattern-scan.yml :(exclude)lazarus_scanner.py"
